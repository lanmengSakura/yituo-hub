# YI TUO HUB GZH CLI 功能实测

这是一篇用于验证 **layout 命令** 的测试文章，包含常用 Markdown 元素。

## 列表与引用

- 支持[本地图片](图片下方)
- 支持**加粗**、*斜体*、`行内代码`
- 支持有序列表

> 引用块：CLI 直连微信接口，只创建草稿，不调用发布。

1. 第一步：写好 Markdown
2. 第二步：选择排版风格
3. 第三步：推入草稿箱

## 配置示例

```bash
curl -fsSL https://cli.yituohub.com/install.sh | bash
```

![测试配图](test-image.png)

正文结束。
